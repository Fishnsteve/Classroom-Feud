import { Difficulty } from './types';

export const DIFFICULTIES: Difficulty[] = [Difficulty.Easy, Difficulty.NotSoEasy, Difficulty.Hard, Difficulty.DeathMode];
export const ROUND_OPTIONS = [3, 5, 7];
export const MAX_STRIKES = 3;
export const ANSWERS_COUNT = 10;
